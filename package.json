{
  "name": "shipment-api-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Outbound API proxy (Node server.js or Cloudflare worker.js)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
