{
  "name": "shipment-api-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Outbound API proxy — third parties see only this server's host and IP",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2"
  }
}
